# Per-job ceilings on the number of tests that may SKIP when a job runs.
#
# Format: <job key><TAB><max skipped tests><TAB># why
# Checked by scripts/check-skip-budget.sh. See that script for the reasoning.
#
# A budget is not a target. Where a job provisions a resource, the right number
# is 0: a skip then means something it set up did not arrive. Where a number is
# larger, it is because the job deliberately does not configure something --
# the engine job runs against PostgreSQL only, so its MySQL and SQL Server
# subtests are "nobody asked", which is the one thing a skip is allowed to mean.
#
# RULE OF THUMB, learned the expensive way four times in one afternoon: any new
# test gated on a dialect costs a skip in every job that does not configure that
# dialect. Two shapes to watch:
#
#   * a test running against registeredBackends costs TWO skips (its mysql and
#     mssql arms) in every PostgreSQL-only job -- test-go/engine AND cluster,
#     which move together;
#   * a test gated on CLEAT_TEST_MYSQL/CLEAT_TEST_MSSQL costs ONE in whichever
#     job runs its package without that dialect -- for ./migration/... that is
#     test-go/support.
#
# Work out which jobs run the package you touched, and bump those, before
# pushing.
# To check without waiting for CI:
#
#   env -u CLEAT_TEST_MYSQL -u CLEAT_TEST_MSSQL -u CLEAT_TEST_POSTGRES \
#     CLEAT_TEST_DB=postgres://... go test -count=1 -json ./engine/... > r.json
#   scripts/check-skip-budget.sh test-go/engine r.json
#
# The absolute number differs from the runner's; the delta does not.
#
# Numbers below were MEASURED on darwin against a live PostgreSQL 16 and, where
# noted, a live MySQL 8.4 -- not estimated. They may still differ by a few on
# the Linux runners, since build tags and toolchain availability change which
# tests exist at all. Reconcile on the first CI run rather than padding them
# now: check-skip-budget.sh prints every skipped test by name when it trips, so
# a mismatch is a one-line fix and not a mystery.

test-go/core	0	# measured: 693 pass, 0 skip
# 347 = 112 mysql + 112 mssql dialect subtests, 119 MySQL/MSSQL-specific
# top-level tests, and 4 Rust toolchain tests. This job configures PostgreSQL
# only (CLEAT_TEST_DB) and installs no Rust -- its Setup Rust step is gated to
# matrix.package.name == 'internal' -- so every one of these is "nobody asked".
#
# Observed on the runner, at 343, after a first attempt seeded from a darwin
# machine that had cargo installed: the four TestRustWorkflow* tests passed
# locally and skip here. The measurement environment was richer than CI, which
# is the failure mode to expect from any locally-seeded budget.
#
# A PostgreSQL problem cannot hide under this ceiling: testutil.TestDB now
# fails rather than skips when a configured DSN is unreachable, so that shows
# up as a failure, not as a larger skip count.
#
# 347 -> 346: TestIntegrationWorkflowMaxDuration used to skip unconditionally
# ("known broken"). It now runs against testdata/spin. Derived, not measured on
# a runner -- the absolute count here cannot be reproduced off-CI, but the delta
# is exactly one test and nothing else changed its skip behaviour. If the first
# CI run disagrees, check-skip-budget.sh prints the exact number.
#
# 346 -> 347: TestFinalizeWorkflowStatus_SQLFenceGuard_MSSQL is new, and skips
# when CLEAT_TEST_MSSQL is unset -- exactly as its _MySQL sibling has always
# done. It closes the 1.1 coverage gap on the third dialect: the guard in
# migrations/mssql/004 shipped with nothing able to detect its removal. A count
# going up needs a reason, and this is one added dialect-conditional test, not
# a test that stopped running.
#
# 347 -> 349: the two 2.71 session-context regression tests, each skipping
# when CLEAT_TEST_MSSQL is unset. They read SESSION_CONTEXT back off a pooled
# connection to prove the tenant survives sp_reset_connection, so they cannot
# run without a real SQL Server. Two tests added, none stopped running.
#
# 349 -> 351: TestMSSQLTenantIsolation_UnderRealSecurityPolicies, two skip
# sites on CLEAT_TEST_MSSQL. It applies the shipped security policies and
# reads across two tenants' pools, so a real SQL Server is not optional.
#
# 351 -> 353: TestMSSQLDeadlock_ClassifiedFromTheRealDriverError, two skip
# sites on CLEAT_TEST_MSSQL. It provokes a real deadlock to check the
# classifier against the error the driver actually returns, so it cannot run
# without a server.
#
# 353 -> 355: TestEnforceParentClosePolicy runs against registeredBackends, so
# its mysql and mssql subtests skip in the Postgres-only engine job.
#
# 355 -> 359: the four MySQL/SQL Server subtests the runner attributes to
# TestPerStepFlushWorksOnEveryDialect, TestPerStepFlushDoesNotDoubleCountEvents
# and TestSignalPayloadRoundTripsOnEveryDialect (IMPROVEMENT-PLAN 2.60). The two
# increments are independent and additive: 355 came from another workstream's
# test, +4 from these.
#
# Measured locally against a clean worktree of develop the delta is +6, not +4.
# The runner's number is the one set here, because the runner is where the guard
# runs, but the discrepancy is unexplained -- so if this fails at 361 that is the
# real number and not a regression. Do not widen it to silence a failure; a
# ceiling that cannot be reached is a guard that cannot fail.
#
# 359 -> 361: TestIdempotencyKeyIsScopedToTenant runs against
# registeredBackends, so its mysql and mssql subtests skip in the Postgres-only
# engine job (IMPROVEMENT-PLAN 3.10). Note this is NOT the +2 the paragraph
# above warns about -- that one is an unexplained local/runner discrepancy, and
# this is two named subtests. Read off two runs of the same guard rather than
# derived: develop at 26efc98 reports `skipped=359 (passed=3114 failed=0)`, this
# branch reports `skipped=361 (passed=3116 failed=0)`. Both counts moved by
# exactly the arm count of one new three-dialect test, and nothing stopped
# running. The postgres arm is the one that matters and it is in the +2 passing.
#
# 361 -> 373: the six three-dialect admin force-resolve tests (IMPROVEMENT-PLAN
# 3.20), each contributing a mysql and an mssql subtest that skip in a job
# configured for PostgreSQL only. The seventh test in that file,
# TestAdminForceResolve_AuditCollisionRollsBack, is PostgreSQL-only by
# construction and adds no skip.
#
# 371 was MEASURED for these twelve, on a runner whose base was 359. 373 is that
# measurement plus 3.10's two, which landed in between -- so the number here is
# derived from two measured deltas rather than read off one run, and the two
# increments are independent and additive. If the guard reports 373, that is the
# confirmation. If it reports something else, that number is the real one and
# this note is what needs correcting -- do not widen it to silence a failure.
#
# 373 -> 374: TestPythonComponentExecutionFence (IMPROVEMENT-PLAN 3.31). It
# builds a Python component that never returns and asserts the configured budget
# is what stops it, so it needs componentize-py, which this job does not install
# -- the "nobody asked" kind of skip. Additive with 3.10's and 3.20's: different
# workstreams, different tests, none of them stopped anything running.
#
# It is not a free skip. e2e-cross-language does install componentize-py and
# declares python in CLEAT_REQUIRE_TOOLCHAINS, where this test Fatals rather
# than skips -- so the component-path fence is verified in exactly one job, and
# that job's budget is 0.
#
# 374 -> 386: the six three-dialect write-ahead call intent tests
# (IMPROVEMENT-PLAN 1.4 phase D), each with a mysql and an mssql subtest that
# skip where neither is configured. Measured as a delta with the same guard on
# the same machine, PostgreSQL only: develop reports `skipped=369
# (passed=3300)`, this branch reports `skipped=381 (passed=3313)`. The absolute
# numbers sit below the runner's -- the discrepancy noted further up,
# unexplained and long-standing -- so the delta is what carries, and it is
# exactly the arm count of the six new tests.
# TestWriteAheadIntent_FailsLoudlyWhenUnsupported needs no backend and adds no
# skip. Additive with the fence test above: the two branches were measured
# independently and touch no test in common.
#
# 386 -> 390: 3.12's two tests, TestDeployDoesNotOverwriteAnotherTenantsDefinition
# and TestDeployRecordsTheDeployingTenant, each with a mysql and an mssql arm
# that skip in the Postgres-only engine job. Measured locally as +4 on the same
# command. Additive with 3.20's twelve, 3.31's fence test and 1.4 phase D's
# twelve: different workstreams, different tests, nothing stopped running.
#
# 390 -> 398: 3.11's four tests -- TestQueueDepthCountsOnlyTheCallersTenant,
# TestGetWASMLengthIsScopedToTenant, TestGetAllowedSignalCallersIsScopedToTenant
# and TestDeleteExpiredEventsDeletesOnlyTheCallersTenant -- each with a mysql
# and an mssql arm that skip in the Postgres-only engine job. OBSERVED on the
# runner rather than derived: the guard reported `skipped=398 budget=390
# (passed=3333 failed=0)`, i.e. zero failing tests and exactly the eight new
# subtests. The three-dialect shape is the point of those tests (3.11 turned
# out to be three dialects, not one), so the arms are not removable; they run
# for real in multi-db-ci.yml's test-mysql and test-mssql jobs.
#
# 398 -> 399: TestMSSQLCreateSchedule_SurvivesTheShippedInputConstraint (3.16),
# one test that skips when CLEAT_TEST_MSSQL is unset. It applies the shipped
# CHECK constraint to workflow_schedules and needs a real SQL Server; it runs
# for real in multi-db-ci.yml's test-mssql job.
#
# 399 -> 401: TestCompleteWorkflowStoresAnObjectForEmptyQueryState (3.17), a
# three-dialect test whose mysql and mssql arms skip here. Observed on the
# runner: `skipped=401 budget=399 (passed=3338 failed=0)`.
#
# 401 -> 403: TestCreateUpdateRequestAcceptsAnyPayload (3.19), a three-dialect
# test whose mysql and mssql arms skip here. Caught before pushing this time,
# by the check documented at the top of this file: the local number moved
# 396 -> 398 on the same command, and the runner sits a fixed distance above
# local, so leaving the budget at 401 would have tripped the guard at ~403.
#
# 403 -> 413: the ten dialect arms of 1.4 phase E's resolver tests --
# TestResolveAmbiguity_CompletesTheStepAndPersistsIt (2),
# _FallsBackWhenItCannotSay's three cases (6) and
# _UnrecordableResolutionIsNotUsed (2) -- each with a mysql and an mssql subtest
# that skip where neither is configured. Measured as a delta with the same guard
# on the same machine, PostgreSQL only: develop reports `skipped=399
# (passed=3372)`, this branch reports `skipped=409 (passed=3383)`. The absolute
# numbers sit below the runner's, as they have all along, so the delta is what
# carries.
#
# 413 -> 417: 3.34's two tests, TestConcurrencyKeyTTLKeepsSubSecondPrecision and
# TestConcurrencyKeyExcludesWhileHeld, each with a mysql and an mssql arm that
# skip here. Measured as a delta rather than assumed: with the file removed the
# same command reports 408, with it 412, so +4 on whatever the runner's base is.
test-go/engine	417	# 413 + 3.34's four dialect arms (local delta 408 -> 412)
test-go/wasm	1	# measured: TestComponentPythonBinary; no python toolchain here
test-go/internal	0	# measured: 463 pass, 0 skip. CI also installs Rust here
test-go/plugins	1	# measured: TestNextRun_Feb29NonLeapYear, a real scheduler gap
#
# 6 -> 7: TestMSSQLPayloadConstraintsAcceptJSONScalars (3.18), which applies the
# real migrations to a scratch database and needs a live SQL Server. Runs for
# real in multi-db-ci.yml's test-mssql job, which has one.
test-go/support	7	# measured: skipped=7 passed=259 with PostgreSQL only
# The four in ./migration/ are all "this job configures PostgreSQL only":
#
#   TestIdempotencyTenantMigrationPreservesExistingKeys/{mysql,mssql}
#   TestRunner_AppliesShippedMySQLMigrations
#   TestRunner_SecondMySQLRunAppliesNothing
#
# 2 -> 4 was the first two, when 3.10 added the upgrade-path test. 4 -> 6 is
# the two MySQL bootstrap tests 3.13 added.
#
# None of them is dead: multi-db-ci.yml's test-mysql and test-mssql jobs now
# run ./migration/... against live servers, which is where they do their work.
# That was the point of adding it there -- a test asserting that the shipped
# migrations can be applied is worth nothing in a job with no server to apply
# them to, and this budget is where that would have been hidden.
#
# The /postgres arm of the upgrade test is absent from the list because
# CLEAT_TEST_DB is set here, so it runs -- and it is the arm that matters,
# since 3.10's upgrade decision is about rows that already exist.
# 4 -> 5: TestTenantIsolationOverHTTP_MSSQL skips unconditionally, blocked on
# 2.71 -- MSSQLStoreFactory's per-tenant sp_set_session_context is cleared when
# a pooled connection is recycled, so with the real schema every tenant-scoped
# read on SQL Server returns nothing. The test is written, applies the real
# migration, and asserts the policies are enabled before asserting anything
# about tenants; it is recorded here rather than deleted so that unskipping it
# is the acceptance test for 2.71. Same treatment as plugin-harness/multi-db.
# Drop back to 4 when it goes.
#
# 5 -> 6: TestTenantIsolationOverHTTP_MySQL. This job configures PostgreSQL
# only, so MySQL is "nobody asked" -- the one thing a skip is allowed to mean.
# It passes against a live MySQL 8.4 and was confirmed to fail with the fix
# reverted. Wiring a MySQL service into this job would take it back to 5.
#
# Note what did NOT move: TestTenantIsolationOverHTTP_Postgres is absent from
# this list because CLEAT_TEST_DB is set for this job, so the database-backed
# tenant isolation test actually runs in CI rather than evaporating.
# 6 -> 7: TestDefersRunOnTheFencedBackend skips unconditionally, and it is the
# uncomfortable kind -- not "nobody asked for a resource" but "this is broken
# and recorded". Every deferred callback runs on wazero regardless of routing,
# because Engine.RunDefer never consults backendForWasm, and wazero does not
# fire the execution fence. The test demonstrates it (a defer that never
# returns is still running 20s into a 1s budget) and is skipped rather than
# left red or deleted, the same treatment TestPythonWasmEndToEnd had.
# Unskipping it is the acceptance test for IMPROVEMENT-PLAN 3.32. Drop back to
# 6 when it goes.
test-go/commands	7	# measured: TestAPIMetrics, TestHelperProcess, TestRunBuild_PythonTarget_WasmRoundtrip, TestTenantIsolationOverHTTP_{MSSQL,MySQL}, plus the note above
# 0 and it should stay 0: this job needs nothing but a Go toolchain, so a skip
# here can only mean the test stopped running.
test-go/manifests	0	# measured: 3 subtests pass, 0 skip
# 0, and it is the whole point of wiring this suite up. Every one of its 68
# tests needs either PostgreSQL or the wasmtime backend, and this job has both
# (CLEAT_TEST_DB, and CGO via -race). Measured on darwin against a live
# PostgreSQL 16 with CGO_ENABLED=1: 68 pass, 0 skip. With CGO off, 7 skip on
# "wasmtime backend requires CGO" -- so a nonzero count here means CGO got
# turned off and the suite quietly stopped testing the primary backend.
test-go/integrity	0	# measured: 68 pass, 0 skip
# 0: everything here needs only the PostgreSQL service this job already runs.
test-go/upgrade	0	# measured: 8 pass, 0 skip
# 0: same, PostgreSQL only. ~23s.
test-go/scale	0	# measured: 16 pass, 0 skip
# 0: the job provisions everything this suite needs -- the same PostgreSQL
# service, and a Go toolchain to build cleat-worker and the fixture. It has no
# conditional skips at all: an unreachable database is t.Fatalf, and the
# wasmtime backend is not optional here either. A skip appearing under this
# ceiling would mean the job stopped providing something.
test-go/crash	0	# measured: 3 pass, 0 skip
fuzz	1	# ./wasm/..., the same TestComponentPythonBinary as test-go/wasm
# Same number as test-go/engine, and for the same reason: this job also runs
# `go test ./engine/...`, against the cluster's own database. It was seeded at 0
# on the assumption that a job which brings up four workers and a database has
# provisioned everything its tests need. That was wrong about *which* tests it
# runs -- ./engine/... carries the whole MySQL and SQL Server suite, and this
# job configures neither. The cluster being healthy is asserted by the
# healthcheck and restart-count steps above this one, not by the skip count.
cluster	417	# same as test-go/engine, and for the same reason; see the note there
# plugin-harness/multi-db is 1, not 0, because TestPluginCalls_MultiDB skips
# unconditionally on a wazero v1.11.1 nil Sys context panic -- so that job
# starts PostgreSQL, MySQL and SQL Server and then tests nothing at all. It is
# recorded rather than zeroed so a known upstream bug does not break the build,
# the same treatment as the deadcode and test-only-code baselines. Drop it to 0
# when the skip is removed.
plugin-harness/multi-db	1
# 1 = TestPluginCalls_Wasm_Python only, which needs componentize-py that this
# job does not install. Go, Rust, AssemblyScript and Java must all run.
#
# 1 -> 0: the entry above said "installing componentize-py here would turn it
# into coverage of the routing that e2e-cross-language currently carries
# alone", and that is what this does. The job now installs componentize-py and
# the Python SDK, so TestPluginCalls_Wasm_Python runs. All five languages run
# in this job and none of them may skip.
#
# Verified on linux before the budget was dropped, not after: 17 plugin
# results, execute and replay identical. It is the only test anywhere that
# drives the whole plugin surface -- streaming included -- from a Component
# Model guest.
#
# The budget is here because the step had no -v and no report, so a skipped
# language was indistinguishable from a passing one -- which is how Go sat
# unconditionally skipped and Rust skipped for want of a toolchain, in a job
# named WASM Integration. Measured on the runner after both were fixed.
plugin-harness/wasm	0
# 0: the Cross-Language E2E workflow installs Rust and the wasm32-wasip1 target,
# so every test in tests/cross-language has what it needs. Measured on darwin
# with cargo present: 7 pass, 0 skip. Without cargo all seven skip, which is
# what this catches.
# 0 -> 1 -> 0. The 1 was TestPythonWasmEndToEnd, skipped as known-broken (2.72)
# after failing on both backends -- wasmtime at "undefined element: out of
# bounds table access", wazero at "module[__main_module__] not instantiated".
# That entry said "drop back to 0 when either instantiation path is fixed;
# unskipping the test is that fix's acceptance test", and that is what happened
# on 2026-08-05: the native component path was being compiled out by a build tag
# no build set, and with it compiled in the same component runs. The test is
# unskipped, so this is a real 0 again -- if Python regresses, this job goes red
# rather than quietly skipping.
e2e-cross-language	0
# 0: the soak job provisions PostgreSQL, which is the only thing this suite
# needs. The one way it can skip is testing.Short(), which nothing in the
# workflow passes -- so a skip here means the job ran for an hour and tested
# nothing, which is precisely the state the suite was already in before it was
# wired up. Measured against a live PostgreSQL 16: 1 pass, 0 skip.
soak	0
