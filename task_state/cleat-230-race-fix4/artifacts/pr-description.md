PR created: https://github.com/cleat-team/cleat/pull/74

Note: there's a warning about 5 uncommitted changes on the branch — the working tree has a modified `engine/engine.go` and some untracked session/task files. The PR includes only the committed change (the race condition fix commit). If those uncommitted changes should also be part of this PR, let me know and I can commit and push them too.
