CONSTANTS
    Workers = {w1}
    NumInstances = 2
    HeartbeatInterval = 1
    HeartbeatTimeout = 2
    DeferPhaseTimeout = 2
    MaxClaimBatch = 1
    NULL = NULL

INVARIANTS
    TypeOK
    Safety

\* L1_EventualSettlement, L2_ClaimProgress and L3_CascadeProgress are
\* defined in the .tla but deliberately NOT gated here -- see the "NOT YET
\* GATED" comment above L1's definition for the concrete, no-mutation-
\* needed counter-examples that stopped them being shipped as checked.
PROPERTIES
    SettledIsFinal

SPECIFICATION Spec
