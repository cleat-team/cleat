CONSTANTS
    Dedupe = FALSE
    NotSentIsSafeRetry = FALSE

INVARIANTS
    TypeOK

\* S1_NoUnwitnessedRedispatch and L1_PendingEventuallySettles are defined in
\* the .tla but deliberately NOT gated here -- see their own "NOT GATED"
\* comments for the concrete, no-mutation-needed counter-examples (the
\* --retention-days sweep of a pending intent) that make them EXPECTED to
\* fail against the current code, not spec artifacts to converge on.
\* S2_DedupeAtMostOnce is structural under Dedupe = TRUE and is verified by
\* a separate, non-shipped local run (see the .tla's own comment) rather
\* than a second .cfg, since Makefile's tla: target pairs one .cfg to one
\* .tla by exact basename.
PROPERTIES
    S3_ResolutionIsDurable

SPECIFICATION Spec
