# CLA signatures

Machine-written. `signatures/version1/cla.json` on this branch is the record of
who has signed the cleat ICLA (docs/cla.md on develop), maintained by
.github/workflows/cla-assistant.yml.

This branch is an orphan and carries no project code. Do not merge it into
develop, and do not add branch protection to it -- the CLA action pushes a
commit here for each signature, and develop rejects direct pushes.