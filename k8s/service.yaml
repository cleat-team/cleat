apiVersion: v1
kind: Service
metadata:
  name: cleat-worker
  labels:
    app: cleat-worker
spec:
  type: ClusterIP
  selector:
    app: cleat-worker
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 8080
