apiVersion: v1
kind: Service
metadata:
  name: durable-worker
  labels:
    app: durable-worker
spec:
  type: ClusterIP
  selector:
    app: durable-worker
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 8080
