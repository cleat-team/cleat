# Cleat SDK
#
# Installed straight from this repo's python-sdk/ rather than PyPI: cleat-sdk
# has not been published yet (cleat#1779, targeted for the 0.3.0 release).
# Once it is, replace this with a version pin like the other dependencies
# below.
cleat-sdk @ git+https://github.com/cleat-team/cleat#subdirectory=python-sdk

# LangChain integration
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0

# Additional providers (optional)
# langchain-anthropic>=0.2.0
# langchain-google-genai>=1.0.0

# Tools
duckduckgo-search>=5.0
