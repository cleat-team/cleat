# My Workflow

Generated by `cleat init --template workflow`.

## Quick Start

1. Build: `make build`
2. Test: `make test`
3. Deploy (requires Postgres): `DATABASE_URL="postgres://..." make deploy`

## Project Structure

- `main.go` — workflow entry point
- `main_test.go` — unit tests
- `cleat.yaml` — workflow metadata
- `Makefile` — build, test, deploy targets
