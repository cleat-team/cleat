# my-fullstack-app

A full-stack cleat application: a durable command path, a browser front-end, and
the plugins that sit between them — wired so the non-obvious settings are
already right.

Generated by `cleat init --template fullstack`.

## Run it

```sh
make up          # postgres + worker
make logs        # confirm the rate limiter mode -- see below
make deploy      # build the workflow to WASM and deploy it
make run         # start one run (needs CLEAT_API_KEY, below)
```

`make up` prints an API key on first start, when no keys exist yet
(`make logs | grep -A2 "CLEAT API KEY"`). Export it as `CLEAT_API_KEY` for
`make run`.

The first run finishes `done`: `status` goes `validating`, `charging` (for three
seconds), `complete`. A bad input (`qty` below 1, no `item`) ends `failed` with an
`invalid order` message instead. Read either back with
`GET /api/workflows/{id}` and `GET /api/workflows/{id}/query?key=status`.

`web/index.html` is the browser half of the same contract, and it does not work
as a file you open: the worker sends no CORS headers, so a browser refuses its
calls from any origin but the worker's own, and it holds no API key. See
"Authentication, and the mistake to avoid" below, and cleat#2307.

Two things that can differ on your machine:

- **Ports.** Postgres is published on 5432 and the worker on 8080. If either is
  taken, set `CLEAT_PG_PORT` / `CLEAT_API_PORT` (for example
  `CLEAT_API_PORT=8081 make up`) and use the same values for every `make` target.
  `web/index.html` still calls port 8080.
- **The worker image.** `docker-compose.yml` uses
  `ghcr.io/cleat-team/cleat-worker:latest`, which is published with each release.
  If `docker pull` says `denied`, no release has published it yet: build it from a
  cleat checkout and point compose at it,
  `docker build -t cleat-worker:local . && CLEAT_WORKER_IMAGE=cleat-worker:local make up`.

## What is wired, and why

| Concern | Component | Note |
|---|---|---|
| Durable command path | `SubmitOrder` in `main.go` | started over HTTP, resumed after a crash |
| Duplicate suppression | `Idempotency-Key` header | a retried request returns the original run |
| Per-tenant rate limiting | `rate-limiter`, **`db` mode** | set in `plugin-config.json`; the default is weaker |
| Front-end | `web/index.html` | no framework, deliberately — see below |

## Check the rate limiter actually took

This is the one setting most likely to be wrong in a way nothing tells you
about.

`rate-limiter` has two modes. The default, `memory`, keeps token buckets in each
worker's process — so with N workers a tenant gets **N times** the configured
rate, and there is no cluster-wide ceiling at all. `db` mode coordinates through
a `rate_counter` table and holds across every worker.

`plugin-config.json` sets `db`, and asking for `db` without a database is an
error: the worker **refuses to start** rather than fall back to per-process
limits (cleat#1581). So a running worker that was given this file is in `db`
mode. Confirm it anyway, because dropping the `--plugin-config` flag from
`docker-compose.yml` silently gives you the `memory` default:

```sh
make logs | grep rate-limiter
```

You want `rate-limiter: initialized mode=db`. If the line says `mode=memory`
(or is absent), the limit you think you configured is not the limit you have.

Two further properties worth knowing before you rely on it as a spend control:
it **fails open** if the database errors — the right default for availability
and the wrong one for billing — and the read-then-increment is not one atomic
statement, so concurrent workers can overshoot slightly at a window boundary.
It is a cluster-wide *limiter*, not a cluster-wide *semaphore*.

## Why the browser does not wait

`SubmitOrder` is a durable run, not a request handler. It may take seconds or
days, it survives a worker dying, and it resumes where it stopped rather than
restarting. None of that fits inside an HTTP response.

So the shape is: **POST to start, then poll published state.** The workflow
calls `SetQueryState`, and the browser reads it back from
`GET /api/workflows/{id}/query?key=status` for one key, or
`GET /api/workflows/{id}/query` with no `key` at all to list everything the
run has published so far.

That read path is intentionally *not* a durable run. Serving reads by starting
workflows would write event history on every page refresh.

## Authentication, and the mistake to avoid

The API key the worker prints identifies the **whole tenant**. It must never
reach a browser.

`web/index.html` calls the API without one, and from the browser, so against the
worker `docker-compose.yml` starts it gets `401` (authentication is on) and, before
that, a CORS refusal (the worker sends no `Access-Control-*` headers; cleat#2307).
It is a sketch of the calls, not something to open from disk. For a real
application, serve your page from your own backend, use the `oauth-provider`
plugin for per-user login, and have that backend call cleat server-side with the
tenant key.

Note the boundary: cleat's row-level security scopes by **tenant**. A user
principal exists at the HTTP layer (`SessionInfo` carries the email), but the
database does not know about it — so per-user authorisation inside a tenant is
your application's job, and the database will not catch a mistake there the way
it catches a cross-tenant one.

## Why there is no framework

The cleat playbooks say your front-end framework *stays rope*: unchanged,
unowned, not reimplemented. A scaffold that picked React or Svelte would
contradict that, and would put an npm toolchain inside a Go project.

`web/index.html` is a single file with no build step. Its job is to show the
**API contract** — start a run, poll state — not to be your application.
Replace it entirely. In production, serve your bundle from a CDN; cleat is the
origin and the API, not your bundler.

## A note on `plugin-config.json`

The worker passes this file's contents to **every** plugin, which each parse the
keys they recognise. There is no per-plugin section, so `{"mode": "db"}` reaches
all of them and only `rate-limiter` acts on it. Keep that in mind if you add a
plugin that wants a key of the same name.

## Next steps

- Replace the placeholder in `main.go`. The `DurableSleepMs(3000)` after
  `charging` stands in for your payment provider; swap it for a durable call, which
  is recorded once and not repeated if the worker dies after it:

  ```go
  resp, err := h.DurableCall("http", "fetch",
      `{"url":"https://payments.example.com/charge","method":"POST","body":"..."}`)
  if err != nil {
      h.SetQueryState("status", "rejected")
      return "", err // ends the run `failed`; a call that exhausts its retries dead-letters
  }
  ```

  Outbound requests must pass cleat's egress policy, which refuses loopback,
  private (RFC 1918) and cloud-metadata addresses, so call a publicly reachable
  endpoint, and stub the call in
  `main_test.go` with `env.OnCall("http", "fetch", nil).Return(...)`.
- Add a compensation path — `cleat.NewSaga` declares forward and undo steps and
  unwinds them for you.
- Put `oauth-provider` in front for per-user login.
- Watch `GET /api/dead-letters` for runs that exhausted their retries; each one
  carries the full history that produced it.
