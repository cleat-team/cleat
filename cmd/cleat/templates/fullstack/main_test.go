//go:build ignore

package main

import "testing"

// A placeholder so `go test ./...` in a fresh scaffold has something to run.
// Replace it with a cleattest harness once SubmitOrder does real work; see
// docs/how-to/test-workflows.md in the cleat repository.
func TestSubmitOrderPlaceholder(t *testing.T) {
	t.Skip("replace with a cleattest harness for SubmitOrder")
}
